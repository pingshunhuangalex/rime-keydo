# 键道·我流自定义设置
# encoding: utf-8

patch:
  char_lookup/comment_format:
    - xform/~[a-z;]+// # 移除以`~`为前缀的逐码提示
  menu/page_size: 2 # 候选项只显示首选与次选
  style:
    inline_preedit: true # 开启输入区嵌入
    label_format: "" # 隐藏候选项序号
    layout:
      margin_y: -12 # 隐藏候选区
    preedit_type: preview # 预览编码
